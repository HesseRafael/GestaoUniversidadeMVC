@model List<DisciplinaModel>
@{
    ViewData["Title"] = "Listagem de Disciplinas";
}

<div class="text-center">
    <div class="d-grid gap-2 d-md-flex justify-content-md-start">
        <a role="button" class="btn btn-outline-success" asp-controller="Disciplinas" asp-action="Adicionar">Adicionar nova Disciplina</a>
    </div>
    <br />
    <h1 class="display-4">Listagem de Disciplinas</h1>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Nome</th>
                <th scope="col">Professor</th>
                <th scope="col">Ação</th>
            </tr>
        </thead>
        <tbody class="table-group-divider">
            @if (Model != null && Model.Any())
            {
                foreach (DisciplinaModel disciplina in Model)
                {
                    <tr>
                        <th scope="row">@disciplina.Id</th>
                        <td>@disciplina.Nome</td>
                        <td>@disciplina.Professor</td>
                        <td>
                            <div class="btn-group" role="group" aria-label="Acoes">
                                <a role="button" class="btn btn-outline-primary" asp-route-id="@disciplina.Id" asp-controller="Disciplinas" asp-action="Alterar">Alterar</a>
                                <a role="button" class="btn btn-outline-danger" asp-route-id="@disciplina.Id" asp-controller="Disciplinas" asp-action="Excluir">Excluir</a>
                            </div>
                        </td>
                    </tr>
                }
            }

        </tbody>
    </table>
</div>
